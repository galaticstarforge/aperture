// @aperture-version 0.1.0
//
// Acceptance criterion #1 — window opens, scrim lifts when onLoad resolves,
// window stays alive until the user closes it.

export async function onLoad() {}

export async function onExit() {}
